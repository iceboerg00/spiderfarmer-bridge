# Spider Farmer Bridge — Mosquitto config
# Wird nach /etc/mosquitto/conf.d/spiderfarmer.conf kopiert.
# Die Default-Werte (persistence, log_dest, pid_file) bleiben in /etc/mosquitto/mosquitto.conf.

# Remote-Zugriff erlauben (HA auf anderem Pi)
listener 1883 0.0.0.0

allow_anonymous true
